
#SPRING CONFIGURATION
 server:
      port: 8085
      
      
 #DATABASE CONFIGURATION
 spring:
   datasource:
    url: jdbc:mysql://localhost:3306/microservices
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
   jpa:
    hibernate:
      ddl-auto: update # Specify the desired DDL strategy
    show-sql: true
 